--[[
    IDENTITY:
    a system that conceals information called "facts" about an entity (name, poritaitm, peronality) until explicitly learned by the player.
]]
local a={}

function a.SET_FACT_ENABLED(fact,a)
    
end

G_Identity=a
